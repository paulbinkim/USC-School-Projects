// Header File for Final Project

// Declaring Volatile Variables (used to initialize counts before EEPROM)
extern volatile int nearCount; // Initial Near Count (Default) 
extern volatile int farCount; // Initial Far Count (Default)
extern volatile int distance; // Used to store distance in MM